Project URL:
https://www.codexstocks.org/

IMPORTANT CONTEXT:
This is a LIVE production PUBG marketplace deployed on Render with a custom domain.

The homepage is indexed and ranking on Google when searching:

* codexstocks

BUT:
The site does NOT rank properly for:

* code x stocks
* codexstock
* PUBG account searches
* individual account titles

Main issue:
Dynamic account pages are not being properly discovered/indexed/ranked by Google.

Example:
https://www.codexstocks.org/account/time-traveler-with-blood-raven-max-8x-sport-cars-under-250k

Even exact title searches do not show the page.

==================================================
IMPORTANT REQUIREMENTS
======================

DO NOT:

* break production
* redesign unnecessarily
* add feature overload
* add heavy animations
* introduce risky migrations
* break auth/admin/chat systems

PRIORITY:

* crawlability
* discoverability
* indexing
* usability
* stability
* clean architecture
* mobile UX
* Google understanding

This is NOT a flashy redesign task.

==================================================
MAIN GOAL
=========

I want CodexStocks to behave like a proper searchable marketplace.

Meaning:

* Google can discover individual account pages
* users land directly on exact listings
* URLs are clean and understandable
* indexing becomes easier and faster
* Google understands the brand properly
* architecture remains simple and scalable

==================================================

1. SLUG-ONLY PUBLIC URL SYSTEM
   ==================================================

Current problem:
Both URLs work:

/account/8
/account/cheapest-72-collection-rare-account-under-50k

I want ONLY clean slug URLs publicly.

Requirements:

* Every new account MUST auto-generate SEO slug from title
* Frontend should ONLY use slug URLs
* Sitemap should ONLY include slug URLs
* Canonical URLs should ONLY use slug URLs
* Internal links should ONLY use slug URLs
* Share URLs should ONLY use slug URLs
* Numeric URLs should NOT appear publicly anywhere

IMPORTANT:
Traffic is still extremely low.
Old shared links are NOT important yet.
So we can safely move toward clean slug-only public architecture.

Keep numeric IDs internally in DB only.

==================================================
2. CREATE /accounts SEO HUB PAGE
================================

Currently there is NO:
https://www.codexstocks.org/accounts

Create it.

Purpose:

* Google crawl hub
* stronger discovery
* internal linking center

Requirements:

* SEO optimized
* mobile optimized
* paginated if needed
* proper H1
* proper metadata
* account cards linking to slug URLs only
* clean fast UI

==================================================
3. STRONG INTERNAL LINKING
==========================

Improve crawl discovery everywhere.

Homepage:

* latest accounts section
* popular accounts section
* direct HTML links to account pages

/accounts:

* links to all active listings

Account pages:

* related accounts
* latest accounts
* similar accounts

Goal:
Googlebot should naturally discover deep pages quickly.

==================================================
4. ACCOUNT PAGE SEO STRUCTURE
=============================

Every account page must have:

* unique title
* unique meta description
* canonical tag
* proper H1
* Open Graph tags
* Twitter tags
* Product schema
* Breadcrumb schema
* meaningful alt text
* readable structured content
* account thumbnail as og:image

IMPORTANT:
Do NOT use generic repeated descriptions.

==================================================
5. BRAND SEO + ENTITY CONSISTENCY
=================================

Current issue:
Google understands:

* codexstocks

But NOT:

* code x stocks
* codexstock

Need to improve brand understanding.

Requirements:

* standardize branding consistently
* use same brand everywhere
* improve Organization schema
* add alternate brand naming support carefully
* improve homepage SEO structure

Goal:
Google should understand:
CodexStocks == Code X Stocks

==================================================
6. VERIFY INDEXABILITY
======================

Fully audit:

* robots.txt
* sitemap.xml
* canonical tags
* noindex issues
* redirect chains
* redirect loops
* HTTP status codes
* Googlebot accessibility
* crawlability

Verify:

* all account pages return proper 200
* no accidental soft 404 behavior

==================================================
7. VERIFY GOOGLEBOT RENDERING
=============================

Very important.

Audit whether:

* metadata is visible in raw HTML
* pages depend too heavily on client-side rendering
* hydration delays affect crawlers

If necessary:

* improve SSR/prerendering carefully
* ensure Googlebot sees content immediately

==================================================
8. SITEMAP IMPROVEMENTS
=======================

Audit sitemap.xml completely.

Requirements:

* slug URLs only
* valid XML
* active accounts only
* proper lastmod
* Google compatible

Also:

* verify robots.txt points correctly to sitemap
* verify Google can fetch sitemap

==================================================
9. DISCOVERY SPEED IMPROVEMENTS
===============================

Goal:
New account pages should get discovered/indexed faster.

Implement:

* better internal linking
* crawl-friendly architecture
* homepage discovery pathways
* listing visibility improvements

==================================================
10. THIN CONTENT AUDIT
======================

Audit whether account pages are too thin/similar.

Improve:

* readability
* uniqueness
* SEO richness
* listing quality

WITHOUT making pages spammy.

==================================================
11. SOCIAL SHARE SEO
====================

Verify:

* WhatsApp previews
* Discord previews
* Facebook previews
* Telegram previews

Each account page should display:

* correct title
* correct image
* correct description

==================================================
12. PERFORMANCE + MOBILE
========================

Keep:

* fast loading
* mobile-first UX
* clean layout
* beginner-friendly flow

DO NOT:

* add unnecessary animations
* add visual clutter
* overload features

==================================================
13. PRODUCTION SAFETY
=====================

IMPORTANT:
This is a LIVE production website.

Before making changes:

* audit carefully
* preserve stability
* avoid risky architecture rewrites
* avoid breaking login/admin/chat systems

Use gradual safe implementation.

==================================================
FINAL EXPECTED RESULT
=====================

I want:

* individual PUBG account pages indexed on Google
* exact account searches opening exact account pages
* clean slug-based URLs everywhere
* stronger Google discovery
* better brand understanding
* scalable long-term SEO structure
* simple clean architecture
* stable production behavior
