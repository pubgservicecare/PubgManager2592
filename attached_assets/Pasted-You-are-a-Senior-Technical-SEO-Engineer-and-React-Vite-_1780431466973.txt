You are a Senior Technical SEO Engineer and React/Vite specialist.

Project: https://www.codexstocks.org

Current Google Search Console status:

* Page indexing: Discovered – currently not indexed
* Last crawl: N/A
* Page fetch: N/A
* User-declared canonical: N/A
* Google-selected canonical: N/A

Current findings:

1. The website is a React/Vite SPA.
2. Homepage contains:

   * meta robots = index, follow
   * valid title and meta description
   * structured data exists
3. robots.txt exists and sitemap is submitted.
4. Sitemap includes:

   * /
   * /accounts
   * /faq
   * /signup
   * dynamic /account/... listing pages
5. robots.txt currently contains:
   Allow: /signup
   Disallow: /signup/
   which creates conflicting signals.
6. Source HTML does NOT contain a server-rendered canonical tag.
7. Canonical appears to be injected dynamically through a React useSEO hook after hydration.

Tasks:

1. Audit the entire SEO implementation.
2. Verify whether canonical tags are present in the initial HTML response sent to crawlers.
3. Ensure every indexable page outputs a valid canonical tag:

   * Homepage → https://www.codexstocks.org/
   * Accounts page → https://www.codexstocks.org/accounts
   * FAQ page → https://www.codexstocks.org/faq
   * Listing pages → self-referencing canonical
4. Remove all robots.txt conflicts.
5. Determine whether signup pages should be indexed. If not:

   * remove them from sitemap
   * keep them blocked from indexing
6. Validate sitemap.xml generation.
7. Verify all sitemap URLs return HTTP 200.
8. Check for soft-404, redirect chains, canonical conflicts, duplicate URLs, and crawlability issues.
9. Check if React client-side rendering is preventing Google from efficiently crawling content.
10. Recommend SSR, prerendering, or SEO improvements if needed.
11. Verify Open Graph, Twitter cards, structured data, robots directives, and canonical implementation.
12. Generate a complete report with:

    * Issues found
    * Root causes
    * Exact code changes required
    * Priority level (Critical / High / Medium / Low)

Do not provide generic SEO advice.
Inspect the actual codebase and identify concrete implementation problems causing Google's "Discovered – currently not indexed" status.
