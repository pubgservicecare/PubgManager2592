Project URL:
https://www.codexstocks.org/

IMPORTANT:
This is a LIVE production PUBG marketplace.

Main issue:
Google is STILL not discovering/indexing most dynamic account pages even after previous SEO improvements.

Current situation:

* Homepage is indexed
* /accounts hub page exists
* sitemap.xml exists
* robots.txt exists
* slug URLs exist
* canonical tags exist
* structured data exists
* internal linking was improved

BUT:
Google Search Console still shows:

* "Discovered – currently not indexed"
* "URL is unknown to Google"
* "Last crawl = N/A"

This means Google still is NOT properly crawling/discovering/rendering/indexing account pages.

==================================================
VERY IMPORTANT
==============

DO NOT:

* redesign UI unnecessarily
* add animations
* break production
* break auth/admin/chat systems
* introduce risky migrations

This task is specifically about:
CRAWLABILITY + DISCOVERABILITY + INDEXING.

==================================================
MAIN GOAL
=========

I want ALL account pages to become:

* crawlable
* discoverable
* indexable
* easy for Googlebot to process

I need a DEEP technical audit focused specifically on:
WHY Google is still not properly crawling account pages.

==================================================
PLEASE AUDIT THESE THINGS CAREFULLY
===================================

1. RAW HTML SOURCE VISIBILITY

---

Very important.

Check:
When opening:
View Source

on:
https://www.codexstocks.org/account/[slug]

Can Google immediately see:

* title
* meta description
* H1
* account description
* JSON-LD
* og:image
* canonical

OR:
Is important content injected only AFTER hydration/client-side JS?

If metadata/content is not visible in raw HTML source,
Google discovery/indexing can fail or slow heavily.

Audit this deeply.

==================================================
2. CLIENT-SIDE RENDERING PROBLEM
--------------------------------

Check whether account pages depend too heavily on client-side rendering.

Potential issue:
Googlebot may receive nearly-empty HTML initially.

If needed:
Implement safe SSR/prerendering strategy for account pages ONLY.

Goal:
Googlebot should instantly see meaningful content without waiting for heavy hydration.

==================================================
3. HTTP STATUS + SOFT 404 AUDIT
-------------------------------

Verify:

* every valid account page returns proper 200
* deleted/inactive pages return proper 404
* no soft-404 behavior exists
* no empty content pages returning 200

==================================================
4. GOOGLEBOT ACCESS TEST
------------------------

Simulate/check:

* Googlebot access
* rendered HTML for crawlers
* blocked assets/scripts
* render failures

Verify Googlebot can fully access:

* images
* metadata
* page content
* schema

==================================================
5. INTERNAL LINK DEPTH
----------------------

Audit whether account pages are buried too deep.

Requirements:

* homepage links to accounts
* /accounts links to all active accounts
* account pages link to related accounts

Goal:
Every important account should be reachable within 2 clicks maximum.

==================================================
6. SITEMAP DISCOVERY AUDIT
--------------------------

Deeply verify:

* sitemap validity
* sitemap accessibility
* correct XML format
* account URLs included
* lastmod validity
* Google fetch compatibility

Verify:
robots.txt correctly references sitemap.

==================================================
7. PAGE QUALITY AUDIT
---------------------

Check whether account pages appear too thin/similar.

Potential issue:
Google may think pages are:

* low-value
* repetitive
* near-duplicate marketplace pages

Improve:

* uniqueness
* readability
* structured information
* meaningful descriptions

WITHOUT keyword stuffing.

==================================================
8. CANONICAL AUDIT
------------------

Verify:
Every account page self-canonicals ONLY to its own slug URL.

No:

* homepage canonicals
* numeric URL canonicals
* conflicting canonicals

==================================================
9. DISCOVERY SPEED IMPROVEMENTS
-------------------------------

Implement production-safe improvements so NEW accounts become discoverable faster.

Potential improvements:

* stronger crawl pathways
* account feed sections
* recent listings modules
* updated content signals

==================================================
10. PERFORMANCE + CRAWLABILITY
------------------------------

Audit:

* slow responses
* render delays
* JS bundle size
* hydration blocking
* crawl inefficiencies

Potential issue:
Googlebot may be timing out or deprioritizing rendering.

==================================================
11. VERIFY WHAT GOOGLE ACTUALLY SEES
------------------------------------

This is VERY important.

I want a REAL audit of:
"What does Googlebot actually receive/render?"

Not assumptions.

Check:

* initial HTML
* rendered DOM
* crawler-visible metadata
* structured data visibility

==================================================
12. FINAL GOAL
==============

I want:

* account pages indexed individually
* exact account searches opening exact account pages
* fast discovery of new listings
* stable long-term SEO architecture
* clean crawl-friendly marketplace structure
* Googlebot-friendly rendering
* production-safe implementation
